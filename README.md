CREATE DATABASE ECOMMERCE_DB;

CREATE TABLE CUSTOMERS(
CUSTOMER_ID INT PRIMARY KEY,
CUSTOMER_NAME VARCHAR(50),
CITY VARCHAR(50),
EMAIL VARCHAR(100));

CREATE TABLE PRODUCTS(
PRODUCT_ID INT PRIMARY KEY,
PRODUCT_NAME VARCHAR(50),
CATEGORY VARCHAR(50),
PRICE DECIMAL(10,2),
STOCK INT);

CREATE TABLE ORDERS(
ORDER_ID INT PRIMARY KEY,
ORDER_DATE DATE,
CUSTOMER_ID INT,
PRODUCT_ID INT,
QUANTITY INT,
TOTAL_AMOUNT DECIMAL(10,2),
FOREIGN KEY(CUSTOMER_ID) REFERENCES CUSTOMERS(CUSTOMER_ID),
FOREIGN KEY(PRODUCT_ID) REFERENCES PRODUCTS(PRODUCT_ID));

INSERT INTO CUSTOMERS VALUES (1,'Ali','Mumbai','Ali@gmail.com'),
                             (2,'Sara','Hyderabad','Sara@gmail.com'),
                             (3,'Hema','Bangalore','Hema@gmail.com');

INSERT INTO PRODUCTS VALUES(101,'SHOES','Fashion',2500,50),
                            (102,'Watches','Accessories',3000,30),
                            (103,'Laptop Bags','Bags',1500,15);



INSERT INTO ORDERS VALUES(1001,'2024-01-10',1,101,2,5000),
                         (1002,'2024-02-12',2,103,1,1500),
                         (1003,'2024-02-15',3,102,1,3000),
                         (1004,'2024-03-03',1,103,2,3000);


